# Integration Architecture

Integration adapters isolate identity, communication, speech, translation, model, knowledge, and healthcare-partner contracts. All integrations use authenticated, versioned interfaces; protect sensitive payloads; handle timeout, retry, duplicate delivery, and outage behaviour; and emit observability and audit signals.
