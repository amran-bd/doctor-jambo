# Communication Patterns

- Synchronous REST or gRPC-style APIs for immediate, bounded queries and commands.
- Asynchronous Kafka events for cross-context propagation and long-running workflows.
- Correlation IDs, consistent error contracts, timeouts, circuit breaking, and retry policy on all remote calls.
- MCP tools are permissioned outbound capabilities for governed agents, never unrestricted execution.
