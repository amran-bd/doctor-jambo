# Deployment Architecture

Docker images run on Kubernetes. Services deploy independently with rolling delivery, health probes, autoscaling, secrets injection, network policies, and isolated namespaces. Managed Kafka, PostgreSQL, Redis, object storage, and vector infrastructure require backups, disaster recovery, and encryption. Production access is controlled through audited, least-privilege operations.
